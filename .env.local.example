# Database
DATABASE_URL="postgresql://user:password@localhost:5432/azora"

# WalletConnect (get from cloud.walletconnect.com)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID="aad5b218b8ad1a4f0d2688cec9941fd3"

# Chain: 97 = BSC Testnet, 56 = BSC Mainnet
NEXT_PUBLIC_CHAIN_ID=97

# JWT secret for API auth (generate with: openssl rand -base64 32)
JWT_SECRET="your-secret-here"

# App URL (used in referral link generation)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
